__pycache__/
*.pyc
.DS_Store
.vscode/
.idea/

# verify_simple.py scratch: screenshots and the headless browser profile
tools/_shots/
tools/_profile/
# storyboard frames pulled from Figma to check a rebuild against — reference,
# not source; re-fetch them from the file rather than committing them
tools/_fig/
# export_video.py scratch: the 4K frame dump and the compiled encoder
tools/_frames/
tools/_bin/
# rendered video is a build product of the deck, not a source of it
dist/*.mp4
