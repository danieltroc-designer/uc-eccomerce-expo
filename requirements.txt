Pillow>=10.0
# Optional, and no longer used by either build: the booth QR is vectorised from
# assets/qr/booth-qr.png rather than generated. Kept because it is the easiest
# way to re-encode a decoded payload and check tools/qr_lib.py read a symbol
# correctly.
segno>=1.6
